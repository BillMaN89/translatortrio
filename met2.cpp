#include <iostream>
#include <list>
#include <string>
#include <stdlib.h>
#include <time.h>

using namespace std;

int main(){
	
	list<string> stringlist = {"ekfrasi"};
	int i=0,count=1;
	srand (time(NULL));
	
	while(i<=stringlist.size()){
		if(stringlist.front()=="ekfrasi"{
			int x =rand() % 2 + 1;
			
		}
	}

	return 0;
}